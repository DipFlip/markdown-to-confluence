[project]
name = "markdown-to-confluence"
version = "0.1.0"
description = "Upload markdown files to confluence pages through API calls"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
    "urllib3>=2.5.0",
]
